<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Hello World</title>
  <link rel='stylesheet' href='/stylesheets/main.css'>
</head>
<body>
  <form action="/" method="get">
    <button>Home</button>
  </form>
  <% if logged_in?%>

    <h1> Hi <%= current_user.email %></h1>
    <a href="/my_deals">My deals</a>
    <a href="/my_account/edit">My Account</a>
      <% else%>
        <h1> <%= session[:user_id] %></h1>
  <% end %>

<%= yield %>

</body>
